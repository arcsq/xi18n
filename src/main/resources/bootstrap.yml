server.port: 8888
spring:
  cloud:
    config:
      server:
        # bootstrap: true
        git:
          uri: file://${user.home}/office/workspaces/poc/language-resources
          # uri: https://bitbucket.org/bangaloreitc/config-repo
          # config-data
          clone-on-start: true
          search-paths:
            - lang
            - language
        prefix: /config
  profiles:
    active: dev
