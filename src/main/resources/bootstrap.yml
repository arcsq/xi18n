server.port: 8888
spring:
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: file://${user.home}/office/workspaces/poc/config-data
        prefix: /config
  profiles:
    active: native
